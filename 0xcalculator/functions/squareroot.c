#include "calc.h"
#include <math.h>
#include <stdio.h>

/**
 * sq_root - calculates the square of a number
 * @a: the input from user
 *
 * Return: the square root of a 
 */

double sq_root(double a){
    return (sqrt(a));
}