#include "calc.h"

/**
 * mul - get the product of 2 numbers
 * @a: first number
 * @b: second number
 *
 * Return: the product
 */
double mul(double a, double b)
{
	double product;

	product = a * b;
	return (product);
}
